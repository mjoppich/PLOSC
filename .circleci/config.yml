# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  mainjob:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: r-base:latest
    resource_class: small
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    environment:
      GITHUB_PAT: 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
    steps:
      - checkout
      - run:
          name: "Deps"
          command: "apt update && apt install -y libcurl4-openssl-dev libssl-dev libxml2-dev libfontconfig1-dev libharfbuzz-dev libfribidi-dev libfreetype6-dev libpng-dev libtiff5-dev libjpeg-dev"
      - run:
          name: "WhichR"
          command: "which R"
      - run:
          name: "Install devtools"
          command: "R -e \"install.packages('devtools', repos = 'http://cran.rstudio.com')\""
      - run:
          name: "Install BiocManager"
          command: "R -e \"install.packages('BiocManager', repos = 'http://cran.rstudio.com')\""
      - run:
          name: "Install Bioconductor packages"
          command: "R -e \"BiocManager::install(c('EnhancedVolcano', 'enrichplot', 'clusterProfiler', 'GSEABase', 'GSVA', 'ComplexHeatmap', 'circlize', 'biomaRt', 'limma'))\""
      - run:
          name: "Install"
          command: "R -e \"devtools::install_github('mjoppich/PLOSC')\""


# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  main:
    jobs:
      - mainjob
